/**
 * Compatibility wrapper for the legacy LLM types import path.
 *
 * The active product path is the local session runtime bridge. Remote
 * provider-specific shapes have been retired from the live web surface.
 */

export * from './types.local-runtime';
