/**
 * Companion component that preserves the legacy SettingsPanel contract while
 * delegating rendering to the local runtime settings surface.
 */

export { SettingsPanel } from './SettingsPanel.local-runtime';
