/**
 * Compatibility wrapper for the legacy SettingsPanel import path.
 *
 * Phase 6 preserves the legacy component contract while routing the old import
 * path to the local-runtime settings surface.
 */

export { SettingsPanel } from './SettingsPanel.compat-local-runtime';
